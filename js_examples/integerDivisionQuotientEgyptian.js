//blueh too much thinking here

function divisionQuoEgypt (big, small, multiplier) {
    if (big < small) {
        return 0;
    } else {
        if ()
    }
    return big < small ?
        0 :
        multiplier + divisionQuo(big - small, small * 2, multiplier * 2);
}

divisionQuoEgypt(31,4,1); //7
